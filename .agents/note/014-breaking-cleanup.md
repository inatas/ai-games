# 需求：未发布阶段移除旧版兼容

Status: implemented

## 需求

清理三层架构调整后不再需要的旧版兼容。

## 范围

2026-09-17用户明确允许代码和数据破坏性升级，不保留过期代码。基线main/1147166为三层架构清理前本地检查点，未push。继续采用[三层设计](../../docs/specs/three-layer-architecture.md)，不改变青溪玩法，也不实施未确认的属性005。

删除mud-core兼容包、storage跨层迁移组合、旧武侠API及路径重映射、游客凭据与提示、旧表回填和旧请求摘要兼容。调用方显式使用platform/game-systems，HTTP仅操作会话当前角色。建表直接描述当前结构；当前版本重复启动不丢数据。

## 验收

- 无旧包依赖、旧API适配或旧数据搬运代码；storage不依赖游戏上层。
- 当前会话隔离、角色重开、消息幂等、任务与库存事务继续通过。
- 空库初始化无旧表/字段；重复初始化不重置NPC位置、物品、角色状态。
- 类型、仓库检查、构建与Docker全量测试通过。
- 必要时显式重建本项目开发库并验证部署；旧账号、存档、消息、流水允许失效，不自动恢复旧备份。

## 当前进展

已删除旧包/路由/配置重映射/游客字段、旧请求摘要兼容及旧表搬运与恢复脚本。storage仅依赖core和pg；MOD显式装配平台与系统建表。历史兼容验收记录仅作历史，不再是当前要求。

Docker两次完整回归61/61通过，类型、仓库检查、前端构建通过。覆盖空库无旧表/字段、重复初始化保留NPC及库存、当前会话请求隔离、多人聊天/队伍/任务、钱包与库存事务。已验证的代码使用标准Dockerfile构建部署，非临时派生镜像。

已核实并清空game-ai-harness项目的harness开发库public schema；清理前账号1、角色1、请求13。新结构账号0、角色0、NPC5，旧表不存在；app/postgres healthy，127.0.0.1:3100首页200、健康接口ok/mock。旧开发记录已删除，没有为本次新建备份；此前.local/backups历史备份仍在，仅可配合历史代码恢复。测试数据库已停止。本次没有push。

## 待完善

本范围已完成。真实支付、跨realm账号IM及属性005仍未实现；本次未重测外部设备访问及真实模型。
