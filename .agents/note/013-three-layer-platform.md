# 需求：三层游戏平台

Status: implemented

## 需求

按用户确认的三层架构调整平台、可选游戏系统与MOD边界。

## 范围

用户明确确认“按照沟通进行最终调整”。契约见[三层设计](../../docs/specs/three-layer-architecture.md)：realm/通信/组队/钱包基础能力；地图/NPC/库存/任务可选系统；青溪规则与内容。支付为可选平台集成，真实渠道未配置不提供充值服务。角色属性005继续待确认。

## 验收

- [x] TL-01～05：依赖边界、无地图平台、策略与事务、钱包、库存。
- [x] TL-06：Docker类型/仓库/前端构建、全量63/63回归、备份隔离恢复及两次迁移通过。部署健康结果见下方记录。

## 当前进展

后续修订：用户已明确放弃旧版兼容，以下兼容包、旧表搬运和恢复脚本记录为历史；当前实现以[需求014](014-breaking-cleanup.md)为准。

检查点main/5507b5f，保存之前NPC/地图拆分、网络配置与属性设计；本轮结果如下。本次未push。

2026-09-17：拆分platform/game-systems，mud-core保留兼容组合入口，应用与青溪显式使用新包。解除平台对房间及任务SQL的依赖，组队退出通过同事务回调处理任务。通用库存接管药包，旧库存一次性迁移；钱包提供账号/角色资产隔离、原子记账、非负余额与幂等流水。真实支付未接入。

最终Docker回归63/63通过（0失败/跳过），包括无地图聊天组队、跨realm收件人拒绝、退出回调回滚、钱包并发扣款与账号资产保留、中性库存隔离及药包迁移不重复发放。构建中TypeScript、依赖边界与Vite检查通过。

开发库备份位于被忽略的`.local/backups/three-layer-before-20260917.dump`，SHA256为`28D2C92FE489267B89E526DC8BAFD7AA5B62724CE0ECD9704617F148C68469AD`。恢复到postgres-test独立库`ai_mud_restore_layers_20260917`后运行`scripts/verify-three-layer-restore.ts`，两次迁移前后原表数量与内容摘要相同：账号1、角色1、请求13、记忆13、NPC5；当时背包为空，非空背包迁移由独立集成测试覆盖。

## 待完善

部署完成：Docker Hub认证请求超时后，从已通过最终63项测试的本地game-ai-harness-tests镜像派生runtime（仅切换NODE_ENV与npm start）。app与postgres均healthy，容器内健康/首页200，宿主127.0.0.1:3100首页200；开发库账号1、角色1、请求13、记忆13、NPC5保持，inventory-1迁移完成。本地派生Dockerfile在被忽略的.local目录，标准Dockerfile仍可在网络恢复后正常构建。本轮未重新验证外部设备访问。

跨realm账号IM、未读/屏蔽/推送、钱包转账冻结、真实充值与退款仍需各自产品方案。青溪银两未转换账本，角色属性005仍为待确认设计，不能把本次分层验收理解为这些功能已上线。
