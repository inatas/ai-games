# AI MUD框架迁移计划

> 历史实施计划：旧数据保留、升级回填与兼容要求已由2026-09-17用户确认的破坏性升级政策取代，不再作为当前执行步骤。

版本v1，已获用户确认。以下为迁移执行顺序与恢复约束；实际执行结果记录在[需求012](../.agents/note/012-ai-mud-framework.md)。

1. 确认根需求012及青溪镇接入需求004、接口/数据/测试范围。同步补齐青溪镇协作委托的具体配置后提交一次完整审阅。
2. 检查当前代码、忽略规则与已有修改，形成实现前本地Git检查点。已确认main/84cee66与实现前工作区一致，作为回退基线，不宣称其已通过多人验收。
3. 先补当前002/003的数据库回归和并发失败用例，定位实际基线；已确认范围内的修复不另扩功能。
4. 新建mud-core公共契约与中性行为测试，再迁移realm、角色、presence、共享NPC及授权投影。
5. 将examples/wuxia-mud迁至mods/qingxi；保留README、note来源和测试ID，规则与内容一起迁移。更新仓库检查器、包导出、服务器和Web调用方，旧位置留下迁移入口。
6. 实现统一共享事务协调、party任务归属、领取防重及青溪镇协作任务。Harness保留玩家scope语义，授权公共事实由宿主显式提供。
7. 通用Web投影和/api/mud接口接入；旧API只作兼容适配，旧请求结果保持可读取。
8. 隔离数据库迁移演练、备份恢复验证、全量Docker与双账号浏览器验收；完成后更新权威文档和实际验收记录。

## 数据迁移与回退

停止接收写请求并处理在途请求后备份数据库，保存校验摘要并在隔离测试库验证恢复。新增realm及角色关联，保留旧scope ID和已提交请求；旧NPC按配置默认值合并到共享realm。若旧档公共NPC状态互相冲突，生成差异报告并阻止自动合并，不任意选择一个玩家状态。个人NPC关系与历史仍归原scope。

先新增与回填，再验证引用、数量和唯一约束，验证成功才切换读写入口；迁移记录版本，可重复执行不重置数据。旧表暂保留只读，不双写两套权威状态。切换失败保持维护状态并回退应用及恢复对应数据库备份；不能仅git回退后继续运行不兼容数据库。

## 交付状态

详细计划已确认并执行。最终数据库备份、隔离恢复、Docker回归及应用部署结果在需求012逐项记录。
